# Optional cloud mode (Supabase)
# Leave empty to keep local-only JSON + URL + localStorage behavior.
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
# Optional for local schema automation script (npm run db:apply-schema)
SUPABASE_DB_URL=

# Optional: better in-app photo search (Unsplash API)
REACT_APP_UNSPLASH_ACCESS_KEY=
